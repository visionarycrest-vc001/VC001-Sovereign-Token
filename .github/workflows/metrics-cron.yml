name: 📡 Metrics Heartbeat

on:
  schedule:
    - cron: "0 0 * * *" # Every 24h

jobs:
  pulse:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Run metrics pulse
        run: node scripts/metrics-pulse.js
